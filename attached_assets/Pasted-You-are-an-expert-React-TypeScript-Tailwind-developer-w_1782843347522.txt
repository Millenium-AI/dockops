You are an expert React/TypeScript/Tailwind developer working on DockOps — a dark-themed marine construction operations platform built for dock building and marine construction field teams.

TECH STACK: React + TypeScript + Tailwind CSS + shadcn/ui + Lucide icons
THEME: Dark mode ONLY. Never use light-mode color classes.

═══════════════════════════════════════════════════════════
DESIGN SYSTEM — USE ONLY THESE TOKENS
═══════════════════════════════════════════════════════════

CSS Variable Tokens:
  Background canvas:    bg-background     (hsl 215 35% 7%  — deep navy)
  Card / panel:         bg-card           (hsl 215 32% 10%)
  Muted surface:        bg-muted          (hsl 215 25% 14%)
  Border:               border-border     (hsl 215 22% 18%)
  Primary text:         text-foreground   (hsl 210 25% 94%)
  Secondary text:       text-muted-foreground (hsl 215 14% 62%)
  Accent teal:          text-primary / bg-primary (hsl 174 58% 48%)

Status utility classes (defined in index.css — use these on badges/pills):
  .status-track   → teal   (on_track / active)
  .status-watch   → amber  (watch / warning)
  .status-risk    → red    (at_risk / overdue)
  .status-neutral → slate  (closed / neutral)
  .status-info    → blue   (informational)

Dark-safe badge color replacements (REQUIRED — never use the left side):
  bg-red-100 text-red-700 border-red-200       → bg-red-950 text-red-300 border-red-800
  bg-amber-100 text-amber-700 border-amber-200 → bg-amber-950 text-amber-300 border-amber-800
  bg-green-100 text-green-700 border-green-200 → bg-emerald-950 text-emerald-300 border-emerald-800
  bg-blue-100 text-blue-700 border-blue-200    → bg-blue-950 text-blue-300 border-blue-800
  bg-violet-100 text-violet-700                → bg-violet-950 text-violet-300 border-violet-800
  bg-slate-100 text-slate-600                  → bg-slate-800 text-slate-300
  bg-gray-100 text-gray-600                    → bg-muted text-muted-foreground
  bg-red-50 border-red-100 text-red-600        → bg-red-950/60 border-red-900 text-red-400
  bg-amber-50 border-amber-200 text-amber-800  → bg-amber-950 border-amber-800 text-amber-300
  bg-green-50 border-green-200 text-green-700  → bg-emerald-950 border-emerald-800 text-emerald-300

Font size replacements (REQUIRED — no hardcoded px font sizes anywhere):
  text-[13px] → text-sm
  text-[12px] → text-sm  or  text-xs
  text-[11px] → text-xs
  text-[10px] → text-[0.625rem]
  text-[10.5px] → text-xs
  text-[22px]  → text-2xl
  text-[12.5px] → text-sm

Spacing: Use Tailwind scale only (p-3, p-4, gap-2, gap-3). No arbitrary spacing values.

Fluid sizing: Use clamp() for widths/heights that should scale with viewport.
  Column widths:   w-[clamp(200px,18vw,280px)]
  Cell min-height: min-h-[clamp(90px,10vh,140px)]

═══════════════════════════════════════════════════════════
FILES TO MODIFY — OUTPUT ALL FOUR IN FULL, NO TRUNCATION
═══════════════════════════════════════════════════════════

1. client/src/pages/Board.tsx
2. client/src/components/JobCard.tsx
3. client/src/pages/Customers.tsx
4. client/src/pages/Schedule.tsx

DO NOT modify any other file.
DO NOT add new npm packages.
DO NOT create new files.
DO NOT modify data types or seed data.
Keep ALL existing TypeScript types, props, and component structure.
All "New Customer", "New Job", QuickBooks sync actions remain as alert() stubs.
Preserve AppShell, JobDrawer, JobCard component usage exactly as-is.
Seed data (CUSTOMERS, JOBS, SCHEDULE_EVENTS, NOW) is read-only.

═══════════════════════════════════════════════════════════
TASK 1 — JobCard.tsx (component used by Board)
═══════════════════════════════════════════════════════════

Fix all colors and font sizes. Specific changes:

1. PermitBadge:
   - overdue:   bg-red-100 text-red-700 border-red-200     → bg-red-950 text-red-300 border-red-800
   - due_soon:  bg-amber-100 text-amber-700 border-amber-200 → bg-amber-950 text-amber-300 border-amber-800
   - ok/green:  bg-green-100 text-green-700 border-green-200 → bg-emerald-950 text-emerald-300 border-emerald-800
   - ok/blue:   bg-blue-100 text-blue-700 border-blue-200   → bg-blue-950 text-blue-300 border-blue-800

2. Blocking issue block:
   bg-red-50 border-red-100 text-red-600 → bg-red-950/60 border-red-900 text-red-400

3. Health dots stay as-is (bg-green-500, bg-amber-400, bg-red-500 — solid colors, fine on dark)

4. Fix all hardcoded px font sizes → Tailwind scale per the table above

5. Keep hover:shadow-md hover:border-primary/30 card effect

═══════════════════════════════════════════════════════════
TASK 2 — Board.tsx
═══════════════════════════════════════════════════════════

1. Fix STAGE_COUNT_COLOR — all dark-safe replacements:
   lead:        bg-slate-100 text-slate-600    → bg-slate-800 text-slate-300
   estimating:  bg-blue-100 text-blue-700      → bg-blue-950 text-blue-300
   contracted:  bg-violet-100 text-violet-700  → bg-violet-950 text-violet-300
   permitting:  bg-amber-100 text-amber-700    → bg-amber-950 text-amber-300
   in_progress: bg-green-100 text-green-700    → bg-emerald-950 text-emerald-300
   closed:      bg-gray-100 text-gray-600      → bg-muted text-muted-foreground

2. Permit alert banner:
   bg-amber-50 border-amber-200 text-amber-800 → bg-amber-950 border-amber-800 text-amber-300
   Icon: text-amber-500 → text-amber-400
   Add a dismiss (×) button that removes the banner from local state

3. Add a summary stat strip between the alert banner and the kanban board:
   - Container: mx-4 mb-3 bg-card border border-border rounded-lg px-4 py-2.5
   - Single horizontal row of 4 stats separated by border-r border-border dividers
   - Stats: Total Jobs (all JOBS count) | Active (stage !== 'closed') | Pipeline Value ($Xk sum of contractAmount where stage !== 'closed') | Closed Revenue ($Xk sum where stage === 'closed')
   - Each stat: muted-foreground label text-xs uppercase tracking-wider + bold text-foreground value text-sm num-display

4. Column width: w-[240px] → w-[clamp(200px,18vw,280px)]

5. Cards scroll area: add max-h-[calc(100vh-220px)] to the overflow-y-auto div so it fills large screens properly

6. Fix all hardcoded px font sizes → Tailwind scale

═══════════════════════════════════════════════════════════
TASK 3 — Customers.tsx
═══════════════════════════════════════════════════════════

3A. Stat strip — replace the 3 static cards with 5 KPI cards in a 5-column grid:
   - Total Customers (CUSTOMERS.length) — icon: Users
   - Active Jobs (JOBS.filter stage !== 'closed') — icon: Briefcase, color text-primary
   - Pipeline Value ($Xk sum contractAmount where stage !== 'closed') — icon: TrendingUp
   - Closed Revenue ($Xk sum where stage === 'closed') — icon: CheckCircle2
   - Avg Job Value (total value / total job count formatted $Xk) — icon: DollarSign
   Each card: bg-card border border-border rounded-lg px-4 py-3
   Label: text-xs text-muted-foreground uppercase tracking-wider
   Value: text-2xl font-bold num-display mt-0.5
   Icon: w-4 h-4 text-muted-foreground float right of label

3B. Customer row redesign — each row includes:
   - Avatar circle (w-8 h-8 rounded-full flex items-center justify-center text-xs font-bold) showing initials (first letter of first + last name). Cycle background through 5 hues based on charCodeAt(0) % 5:
     0 → bg-teal-900 text-teal-300
     1 → bg-blue-900 text-blue-300
     2 → bg-slate-700 text-slate-300
     3 → bg-violet-900 text-violet-300
     4 → bg-amber-900 text-amber-300
   - Customer name text-sm font-semibold + source tag inline (bg-muted text-muted-foreground text-xs px-2 py-0.5 rounded)
   - Email and phone as icon+text links (keep existing pattern)
   - Location: city only in list row
   - "Last Job" column: most recent job date formatted as "Jun 15" or "No jobs" in text-muted-foreground/50
   - "Tags" column — auto-derived:
     Show "Repeat" badge (status-neutral) if customer has >1 closed job
     Show "Active" badge (status-track) if any open jobs exist
     Show "New" badge (status-info) if only 1 job total and stage is lead or estimating
     Multiple tags can show — render as flex gap-1
   - Job count: teal pill for open jobs, muted for closed (keep existing pattern, fix colors to dark-safe)
   - Total value column (keep existing)
   - Quick Actions cell — visible on row hover (group-hover:flex, default hidden):
     Three icon buttons: Mail (mailto:), Phone (tel:), PlusCircle ("New Job" alert stub), ExternalLink (QB alert stub)
     Styled: text-muted-foreground hover:text-primary transition-colors w-3.5 h-3.5
     Remove the separate QB column entirely — it moves here

3C. Expanded sub-rows — fix colors:
   - Stage badges: replace any hardcoded color classes with status-track / status-watch / status-risk / status-neutral based on job health or stage
   - "Open Job" button replacing ChevronRight: text-xs text-primary hover:underline cursor-pointer

3D. Search and filter bar:
   - Keep existing search input
   - Add "Filter by source" dropdown — options derived from unique source values in CUSTOMERS data (plus "All Sources" default)
   - Add "Sort by" dropdown: Name A–Z | Name Z–A | Most Jobs | Highest Value | Most Recent
   - Apply sort+filter logic in the useMemo that produces `filtered`
   - "New Customer" button stays right-aligned as alert() stub

3E. Empty state — when filtered.length === 0:
   Replace the plain "No customers match" text cell with a centered empty state:
   - Users icon w-10 h-10 text-muted-foreground/30 mx-auto mb-3
   - Heading: "No customers found" text-sm font-medium text-muted-foreground
   - Subtext: "Try adjusting your search or filters" text-xs text-muted-foreground/60

3F. Fix ALL hardcoded px font sizes → Tailwind scale
3G. Fix ALL light-mode color classes → dark-safe replacements per the table above
3H. Fix all arbitrary spacing → Tailwind scale

═══════════════════════════════════════════════════════════
TASK 4 — Schedule.tsx
═══════════════════════════════════════════════════════════

4A. View toggle — replace 2-way "calendar | list" with 3-way toggle: "Board | Calendar | List"
   Default to "Board". Same styling pattern as existing toggle (p-0.5 border border-border bg-card rounded-md).
   Icons: Board → LayoutGrid, Calendar → CalendarDays, List → List (all from lucide-react)

4B. Board view (NEW — crew swim-lane dispatch):
   Layout:
   - Outer container: overflow-x-auto
   - Left column fixed w-36 shrink-0: one row per lane
   - Right section: horizontal scroll showing 7 days as columns
   - Lane rows: ["Unassigned", "Crew A", "Crew B", "Crew C", "Crew D"]

   Day column headers (sticky top-0 z-10 bg-background):
   - Show weekday abbrev + date: "Mon\n30" stacked, text-xs text-muted-foreground
   - Today column header: text-primary font-semibold, teal bottom border

   Lane label cells (left column):
   - bg-muted/30 border-r border-border px-3 py-2 text-xs font-medium text-muted-foreground
   - "Unassigned" label in text-muted-foreground/50 italic

   Event chips in cells — dark-safe colors:
   - site_visit:        bg-slate-800 border-l-2 border-slate-400 text-slate-200
   - permit_deadline:   bg-red-950 border-l-2 border-red-500 text-red-300
   - material_delivery: bg-blue-950 border-l-2 border-blue-400 text-blue-200
   - install_start:     bg-emerald-950 border-l-2 border-emerald-500 text-emerald-200
   - install_finish:    bg-emerald-900 border-l-2 border-emerald-400 text-emerald-300
   - inspection:        bg-amber-950 border-l-2 border-amber-400 text-amber-200

   Each chip: flex items-center gap-1.5 px-2 py-1 rounded text-xs font-medium truncate cursor-pointer hover:opacity-80
   Shows: type icon + title (truncated)
   onClick → setSelectedEvent(event)
   Events with no crew → "Unassigned" lane
   Show 7 days starting from today

4C. Calendar view (upgrade existing):
   - Show 28 days (4 full weeks) starting from the Monday of the current week
   - Month/year header above grid: "June – July 2026" with ← → week navigation arrows (prev/next 7 days)
   - Day-of-week column headers: Mon Tue Wed Thu Fri Sat Sun
   - Cell min-height: min-h-[clamp(90px,10vh,140px)]
   - Today cell: ring-1 ring-primary bg-primary/5
   - Past cells: opacity on events only (not the cell itself) — wrap events in <div className="opacity-40">
   - Event chips: use the same dark-safe chip colors from 4B above
   - Each chip shows: icon + title + crew pill (text-[0.625rem] bg-muted text-muted-foreground px-1 rounded)
   - Keep drag-to-reschedule (existing onDrop/onDragStart logic)
   - Keep "Drop here" target indicator

4D. List view (upgrade existing):
   - Group events by date with sticky section headers:
     Format: "Monday, June 30 · 3 events"
     Header style: bg-muted/30 px-4 py-1.5 text-xs font-semibold text-muted-foreground uppercase tracking-wider sticky top-0 z-10
   - Within each group: sort by crew then type
   - Fix all event row colors to dark-safe classes
   - Add crew badge per row — colored pill:
     Crew A → bg-teal-900 text-teal-300
     Crew B → bg-blue-900 text-blue-300
     Crew C → bg-amber-900 text-amber-300
     Crew D → bg-slate-700 text-slate-300
     Nick   → bg-violet-900 text-violet-300
     No crew → omit badge
   - "days until" countdown styling:
     ≤ 2 days: text-red-400 font-bold
     3–7 days: text-amber-400 font-semibold
     > 7 days: text-muted-foreground

4E. "Today at a Glance" strip — always visible above the view toggle:
   - Container: bg-card border border-border rounded-lg px-4 py-2.5 mb-3 flex items-center gap-3
   - Left label: text-xs font-semibold text-muted-foreground uppercase tracking-wider "Today" — w-12 shrink-0
   - Right: horizontal scrollable flex gap-2 (overflow-x-auto no-scrollbar)
   - Shows today's events as mini chips using the same dark-safe chip colors
   - Each chip: type icon + title truncated max-w-[160px]
   - If no events today: "Nothing scheduled today" text-xs text-muted-foreground/50 italic

4F. Permit alert strip (upgrade existing):
   - Fix colors: overdue → bg-red-950 border-red-800 text-red-300, icon text-red-400
   - Fix colors: due_soon → bg-amber-950 border-amber-800 text-amber-200, icon text-amber-400
   - Add dismiss (×) button per alert: onClick removes that event id from a local dismissedAlerts Set state
   - Show date prominently: "OVERDUE · Jun 22" or "Due Jun 28 · in 3 days"

4G. Fix ALL hardcoded px font sizes → Tailwind scale
4H. Fix ALL light-mode color classes → dark-safe replacements
4I. Fix all arbitrary spacing → Tailwind scale

═══════════════════════════════════════════════════════════
FINAL CHECKLIST BEFORE OUTPUTTING
═══════════════════════════════════════════════════════════

Before writing your output, verify:
[ ] Zero instances of bg-red-50, bg-amber-50, bg-green-50, bg-blue-50, bg-slate-100, bg-gray-100, bg-violet-100, bg-green-100 remain
[ ] Zero instances of text-red-700, text-amber-700, text-green-700, text-blue-700, text-gray-600, text-slate-600, text-violet-700 remain
[ ] Zero instances of text-[Npx] remain (all replaced with Tailwind text scale)
[ ] All four files output in full with no "// ... rest unchanged" truncations
[ ] No new imports beyond lucide-react icons already used in the codebase
[ ] AppShell, JobDrawer, JobCard component APIs unchanged
[ ] CUSTOMERS, JOBS, SCHEDULE_EVENTS, NOW seed data only read, never mutated